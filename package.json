{
  "name": "kids-activity-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Generate printable educational activity worksheets for kids.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist",
    "generate:coloring": "node scripts/generate-coloring.mjs",
    "optimize:coloring": "node scripts/optimize-coloring.mjs"
  },
  "dependencies": {
    "jspdf": "^2.5.2"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "potrace": "^2.1.8",
    "vite": "^5.4.10"
  }
}
